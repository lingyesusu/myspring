<?xml version="1.0" encoding="UTF-8" ?>  
<!DOCTYPE mapper PUBLIC "-//ibatis.apache.org//DTD Mapper 3.0//EN"      
 "http://ibatis.apache.org/dtd/ibatis-3-mapper.dtd">
<mapper namespace="com.dao.UserDao"><!-- 映射到哪个接口 -->
	<select id="getAll" resultType="com.entity.User">
		select * from user
	</select>
	
	<insert id="add">
		insert into user(username,password,deptId,roleId,age,name,status) values (#{username},#{password},#{deptId},#{roleId},#{age},#{name},#{status});
	</insert>
	
	<select id="getById">
		select username,password,deptId,roleId,age,name,status from user where id=#{id}
	</select>
	
	<delete id="delById">
		delete username,password,deptId,roleId,age,name,status from user where id=#{id}
	</delete>
	
	<update id="updateById">
		update user set username,password,deptId,roleId,age,name,status where id=#{id}
	</update>

</mapper>
